@model IEnumerable<FinalYear.Models.PODetail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.pageTitle = "Manage Purchases";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProDetail.ProUnit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.product.ProName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.purchase.PurchaseId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Supplier.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProDetail.ProUnit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.product.ProName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.purchase.PurchaseId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Supplier.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
